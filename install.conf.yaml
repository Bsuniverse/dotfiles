- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: 
    ~/.zshrc: 
    ~/.vimrc: 
    ~/.vim:
    ~/.oh-my-zsh:
    ~/.zshcustom:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.inputrc:
    ~/.pythonrc:
    ~/.tmux.conf:
    ~/.tmux.conf.local:

- shell:
    - [git submodule sync --recursive, Installing submodules]
    - git update-submodules
