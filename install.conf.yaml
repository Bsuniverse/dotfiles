- defaults:
  link:
    relink: true

- clean: ['~']

- link:
  ~/.bashrc: 
    relink: true
    path: bashrc
  ~/.zshrc: 
    relink: true
    path: zshrc
  ~/.vimrc: 
    relink: true
    path: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
