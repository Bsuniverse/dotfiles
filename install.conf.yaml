- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: 
    ~/.zshrc: 
    ~/.vimrc: 
    ~/.gitconfig:

- shell:
    - [git submodule sync --recursive, Installing submodules]
    - git update-submodules
