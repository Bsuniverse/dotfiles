- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: 
    ~/.zshrc: 
    ~/.vimrc: 
    ~/.vim:
    ~/.oh-my-zsh:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.inputrc:
    ~/.pythonrc:
    ~/.tmux.conf:

- shell:
    - [git submodule sync --recursive, Installing submodules]
    - git update-submodules
